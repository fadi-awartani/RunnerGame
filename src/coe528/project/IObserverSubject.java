package coe528.project;

/**
 * A Marker interface.
 * Uses the marker interface design pattern.
 * This interface 'marks' classes which implement it, as subjects (for other observer objects).
 * As such, only marked subjects can update their observers, and the observer can
 * perform an action based on the type of the implementor class that updated it.
 * @author Aaron, Anjalo, Fadi
 */
public interface IObserverSubject {   
}
